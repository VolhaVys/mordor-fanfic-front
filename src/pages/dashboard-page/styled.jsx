import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles((theme) => ({
  gridTitle: {
    padding: '40px 0 30px',
  },
  tagCloud: {
    marginTop: 30,
    maxWidth: 500,
    margin: 'auto',
    textAlign: 'center',
  },
}));
